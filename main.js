const second_form = document.querySelector("#second-form");

const lastSubmit = document.querySelector("#second-submit");

second_form.addEventListener("submit", (e) => {
  e.preventDefault();
  console.log(submit);
});
